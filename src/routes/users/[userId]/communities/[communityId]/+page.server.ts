import type { PageServerLoad } from './$types';

// Fetch projects in this community
// Include user's role in each project

export const load = (async () => {
	return {};
}) satisfies PageServerLoad;
